export enum NotificationType {
  REMINDER = 'REMINDER',
  STATUS_CHANGE = 'STATUS_CHANGE',
  INFO = 'INFO',
}

export enum NotificationStatus {
  PENDING = 'PENDING',
  READ = 'READ',
  DISMISSED = 'DISMISSED',
}
